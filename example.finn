def add_one (a: Int) {
    return: a + 1;
}

def check (string: Str ) {
    mut file = 10;
    if (string != "hello") {
        file += 100
    }
    return: file;
}

def load (filename: str) {
    let file := filename;
    return: file;
}

def pipe := add_one . check . load
